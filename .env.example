# ============================================================
# GitHub Copilot モデル選択ナビゲーター - 環境変数設定
# ============================================================
#
# このファイルをコピーして .env を作成し、
# 実際の設定値に置き換えてください。
#
# 初期セットアップ:
#   cp .env.example .env
#

# ============================================================
# 必須設定
# ============================================================

# Google Gemini API Key
# 用途: データ更新時のモデル情報解析に使用
# 取得方法:
#   1. https://aistudio.google.com/ にアクセス
#   2. 社内 Google アカウントでログイン
#   3. 左側メニューから "Get API Key" クリック
#   4. 新規APIキーを作成し、ここに貼り付け
GEMINI_API_KEY=your-gemini-api-key-here

# ============================================================
# オプション設定
# ============================================================

# 組織名（Webアプリに表示）
# デフォルト: "Organization"
ORGANIZATION_NAME=Your Company Name

# データ更新時に使用するLLMモデル
# 推奨値: gemini-2.5-flash-lite (デフォルト・最速)
# オプション:
#   - gemini-2.5-pro       (最高品質・デフォルト)
#   - gemini-2.5-flash     (高速・低コスト)
#   - gemini-2.5-flash-lite (最速・最低コスト)
# 変更後は以下でバックエンドを再起動:
#   docker compose up -d backend
LLM_MODEL=gemini-2.5-flash-lite

# LLM パラメータ（通常は変更不要）
LLM_TEMPERATURE=0.3
LLM_MAX_TOKENS=8192

# ============================================================
# Docker 環境設定（通常は変更不要）
# ============================================================

# Redis接続URL
REDIS_URL=redis://redis:6379

# Database URL
DATABASE_URL=sqlite:///data/app.db

# スクレイピング設定
SCRAPE_TIMEOUT=30
SCRAPE_MAX_RETRIES=3
# Organization settings (オプション: 社内向け設定)
ORGANIZATION_NAME=Your Company Name
ENABLE_USAGE_ANALYTICS=false
